% Fonts
\usepackage{dsfont}
\usepackage{amsmath}
% Algotirhms 
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
% Pretty Tables, not breaking tables across pages
\usepackage{longtable}
% Unicode
\usepackage[utf8]{inputenc}
% Add pdf pages
\usepackage{pdfpages}

% ArgMax operator
\DeclareMathOperator*{\argmax}{arg\,max}

% Environment to use before bookdown
\newenvironment{preliminary}%
{\pagestyle{empty}\pagenumbering{roman}}%
{\pagenumbering{arabic}}

% Don't  print apage number
%\newenvironment{dontprintpage}
%{\thispagestyle{empty}}

%  Turns off hyphenation of words across multiple lines
\usepackage[none]{hyphenat}
% Removes section headings from the top of pages
\pagestyle{plain}
% Prevents LaTeX from varying the spacing between paragraphs to minimise empty space on pages
% \raggedbottom

% Pretty Chapters
\usepackage{quotchap}

% Quotations
\usepackage{dirtytalk}
